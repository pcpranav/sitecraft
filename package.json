{
  "name": "sitecraft",
  "version": "1.0.0",
  "description": "An AI-powered website builder built with Claude. Describe any idea and get a complete multi-page website with HTML, CSS, and JS — ready to iterate, export, or deploy.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --signal SIGTERM --watch netlify/functions --watch index.html --ext mjs,html,js,json --exec 'netlify dev'",
    "start": "netlify dev",
    "deploy": "netlify deploy --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.101.0"
  },
  "devDependencies": {
    "netlify-cli": "^24.8.1",
    "nodemon": "^3.1.14"
  },
  "engines": {
    "node": ">=20.12.2"
  }
}
